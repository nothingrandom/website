---
layout: layouts/base.njk

title: Resume.

permalink: /resume/index.html
---
<div class='section section--standard section--cv'>
  <div class='row center-md'>
    <div class='col-xs-12 col-md-6'>
      <div class='section__header text-left'>
        <h2>{{ title }}</h2>
      </div>
      <div class='section__intro text-left'>
        <p>I'm Benjamin Hollway, a lead frontend / Javascript engineer based {{ myself.currentLocation }}. And a lover of good food.</p>
        <p>{{ myself.currently | safe }} {{ myself.fe }}</p>
      </div>
    </div>
  </div>
  <div class='row center-md row--md-padding'>
    <div class='col-xs-12 col-md-6'>
      {% include 'partials/cv-social.njk' %}
    </div>
    <div class='col-xs-12 col-md-8'>
      <ul class='cv__list text-left'>
        {% for experience in resume %}
          <li class='cv__item'>
            <div class='cv__item__header'>
              <h3 class='font--bold'>{{ experience.role }}</h3>
              <div class='flex-spread'>
                <h4>{{ experience.company }}</h4>
                <span class='section__item__detail'>{{ experience.location }}<br />{{ experience.start | cvDate }} - {% if experience.end %}{{ experience.end | cvDate }}{% else %}Present{% endif %}</span>
              </div>
              {% for para in experience.description %}
                <p>{{ para | safe }}</p>
              {% endfor %}
            </div>
          </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</div>